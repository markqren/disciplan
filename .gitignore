.DS_Store
*.swp
data/
node_modules/
references/

# Local Netlify folder
.netlify

# Local DB dumps (sensitive)
backups/

# Local secrets (Supabase PAT for scripts/dbq.sh, etc.) — never commit
.secrets/
*.pat
.env.local
